## f_tell

f_tell 函数获取文件的当前读/写指针。

```c
FSIZE_t f_tell (
  FIL* fp   /* [输入] 文件对象 */
);
```

### 参数

*   **fp**: 指向已打开的文件对象结构的指针。

### 返回值

返回文件的当前读/写指针。

### 描述

在此版本中，`f_tell` 函数是作为宏实现的。它没有任何验证和互斥。

```c
#define f_tell(fp) ((fp)->fptr)
```

### 快速信息

始终可用。

### 相关参考

`f_open`, `f_lseek`, `FIL`
